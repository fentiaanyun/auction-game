# 🚀 臻藏拍卖系统 - Firebase 部署指南（Hosting Only）

本项目采用 **Vite + Firebase Hosting**，并使用 **Realtime Database + Anonymous Auth** 进行云同步（默认开启）。

> 说明：本仓库只自动部署 Hosting（不通过 CI 发布数据库规则/索引）。数据库规则请在 Console 中设置。

---

## ✅ 部署总览

- **本地开发**：`npm run dev`
- **本地部署**：`npm run deploy`
- **自动部署**：push 到 `main` 后由 GitHub Actions 发布到 Firebase Hosting

---

## 1）Firebase Console 新建项目（一次性）

1. 创建项目
2. 启用 **Hosting**
3. 启用 **Realtime Database**
4. 启用 **Authentication -> Anonymous**
5. Realtime Database 规则设置为（建议）：

```json
{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null"
  }
}
```

仓库文件：`database.rules.json`

---

## 2）配置环境变量（云同步开关）

云同步是否启用由 Firebase Web 配置决定：

1. 复制 `env.example` 为 `.env.local`
2. 填入你新建项目的 Web App 配置
3. 运行 `npm run dev` 即可自动启用云同步

> `.env.local` 不要提交到仓库（已在 `.gitignore` 中忽略）。

---

## 3）本地一键部署到 Hosting（可选）

```bash
npm install
npx firebase-tools login
npx firebase-tools use --add
npm run deploy
```

---

## 4）GitHub Actions 自动部署（推荐）

### 4.1 Secrets 列表

在 GitHub 仓库配置以下 secrets：

- `FIREBASE_SERVICE_ACCOUNT_JSON`
- `VITE_FIREBASE_API_KEY`
- `VITE_FIREBASE_AUTH_DOMAIN`
- `VITE_FIREBASE_DATABASE_URL`
- `VITE_FIREBASE_PROJECT_ID`
- `VITE_FIREBASE_APP_ID`

### 4.2 工作流

工作流文件：`.github/workflows/firebase-hosting-deploy.yml`

触发方式：push 到 `main` 分支。

