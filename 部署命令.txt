# 部署命令（复制粘贴执行）

# 第 1 步：连接 GitHub 仓库
# 替换 你的用户名 为实际的 GitHub 用户名
git remote add origin https://github.com/你的用户名/auction-game.git

# 第 2 步：推送到 GitHub
git branch -M main
git push -u origin main

# 注意：执行 git push 时，浏览器会自动打开要求登录 GitHub（OAuth，不需要密码）


