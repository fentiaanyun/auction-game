{
  "name": "auction-game",
  "private": true,
  "version": "4.0.0",
  "description": "臻藏拍卖 - 现代化艺术品竞拍平台（Vite + Firebase Hosting）",
  "scripts": {
    "dev": "vite --port 8000",
    "build": "vite build",
    "preview": "vite preview --port 8000",
    "deploy": "npm run build && firebase deploy --only hosting"
  },
  "dependencies": {
    "firebase": "^12.6.0"
  },
  "devDependencies": {
    "firebase-tools": "^13.23.0",
    "vite": "^6.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
